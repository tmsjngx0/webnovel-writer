# AGENTS.md — 이 저장소의 에이전트 규칙

이 저장소는 한국어 무협 장편 웹소설의 집필 저장소다. 이 파일은 **모든 에이전트가 읽는 공통 규칙**과 **`codex exec` 단계 실행 규칙**을 담는다. Claude Code 전용 진행 규칙은 `CLAUDE.md`에 있다.

## 공통 규칙 (Claude Code, 대화형 Codex 모두)

### 호칭

모든 응답은 "선배," 로 시작할 것. 말투는 공손체(~ㅂ니다 / ~요)로 작성한다. "선배" 호칭과 매칭되도록 반말 금지.

출처는 `dotfiles/common/ai/AGENTS.md`(devops) 1행이다. 그 파일이 `~/.config/ai/AGENTS.md`로 배포되고 `~/.claude/CLAUDE.md`가 그리로 심링크되지만, lunaw는 ansible 인벤토리에 없어 배포가 닿지 않는다. 그래서 여기에 적어 둔다.

### 커밋

1. **커밋 메시지에 AI attribution을 넣지 않는다.** `Co-Authored-By: Claude ...`, `Claude-Session: ...`, `Generated with Claude Code`, 로봇 이모지 전부 금지. 이 규칙은 도구 기본값보다 우선한다.
2. **논리 변경 하나당 커밋 하나.** 원고 + 규칙 문서 + harness 수정이 섞여 있으면 세 커밋으로 나눈다.
3. **`--no-verify`를 쓰지 않는다.** 사람이 명시적으로 지시할 때만 예외다.
4. **의도 없는 `--amend`를 하지 않는다.** 히스토리를 고치는 대신 새 커밋을 쌓는다.
5. **pre-commit 훅이 실패하면 커밋은 일어나지 않은 것이다.** 고치고, 다시 stage하고, `--amend`가 아니라 새 커밋을 만든다.
6. Git 커밋은 사람이 요청할 때 한다. `python harness/wn.py commit`은 Git 커밋이 아니라 `state/events.md` 반영이다. 둘을 섞어 말하지 않는다.

### 세션 종료

세션을 끝내기 전에 넷 다 끝낸다. 선택 사항이 아니다.

1. **변경을 커밋하고 `git push`가 성공할 때까지 끝내지 않는다.** 문서나 원고 수정을 커밋하지 않은 채 세션 경계를 넘기지 않는다.
2. **남은 일·미해결 지적은 GitHub 이슈로 남긴다.** 다음 세션이 대화 기록 없이도 이어갈 수 있어야 한다.
3. **회차 작업은 receipt 갱신까지 끝낸다.** 한 세션에 한 회차만 처리한다.
4. 새로 알게 된 운영상 사실은 해당 문서(`docs/`)에 반영한다. 대화에만 남기지 않는다.

## `codex exec` 단계 실행 규칙

Codex는 `harness/wn.py`가 만든 프롬프트로 `codex exec`를 통해 한 단계씩 호출된다. 아래는 그 단계 실행에만 해당한다. 진행과 사람과의 대화는 Claude Code가 맡는다(`CLAUDE.md`).

### 단계 실행 규칙

1. **프롬프트에 포함된 자료만 근거로 삼는다.** 저장소의 다른 파일(`story/outline.md` 전체, `docs/`, `runs/`의 다른 실행, `legacy/`)을 찾아 읽지 않는다. 프롬프트의 자료는 이번 단계에 필요한 범위로 잘라 둔 것이다.
2. **파일을 만들거나 고치지 않는다.** 결과는 최종 메시지로만 낸다. harness가 최종 메시지를 `runs/<run_id>/output.md`에 저장한다.
3. **새 설정은 제안일 뿐이다.** 새 고유명사·무공·과거사·규칙이 필요하면 출력의 "설정 추가 필요/의심" 절에 적는다. 확정 사실처럼 쓰지 않는다.
4. **세계의 진실, 인물의 주장, 인물의 믿음, 독자에게 공개된 정보를 섞지 않는다.** 장로가 "배신했다"고 말한 것은 발언이지 사실이 아니다.
5. 한국어로 쓴다. 기술 용어는 영어를 써도 된다.
6. 단계별 형식은 프롬프트 앞부분의 지시를 따른다. 형식이 깨지면 자동 검사에서 거부된다.

### 자료 위치 (참고용, 직접 읽지 말 것)

| 파일 | 의미 |
|---|---|
| `story/story-contract.md` | 작품의 핵심 재미와 방향 |
| `story/canon.md` | 확정(또는 승인 대기) 설정. `집필제외` 구간은 검수 단계에만 전달된다 |
| `story/style.md` | 승인된 문체 예시와 수정 이유 |
| `story/outline.md` | Arc와 회차 계획 |
| `story/relations.md` | 관계 자유 서술 |
| `state/events.md` | 확정 원고에서 추출해 사람이 승인한 사건·상태 기록 |
| `drafts/` | 원고 후보 |
| `chapters/` | 확정 원고 |
